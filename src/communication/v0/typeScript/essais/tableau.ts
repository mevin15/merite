import {
    Unite
} from "../bibliotheque/types";

let tableau = [1, 2, 3, 4];

console.log(tableau); // !> [ 1, 2, 3, 4 ]

let tab : number[] = [];
tab[0] = 0;
tab[1] = 1;
tab[2] = 2;
console.log(tab.toString()); // pas d'erreur !